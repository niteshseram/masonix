// Playground — exercises masonix during development.
// Import directly from source via the alias in playground/vite.config.ts.

export default function App() {
  return (
    <div style={{ padding: 24 }}>
      <h1 style={{ marginBottom: 8 }}>masonix playground</h1>
      <p style={{ color: "#666", marginBottom: 24 }}>
        Components will be wired up as they are built in Phase 2.
      </p>
    </div>
  );
}
