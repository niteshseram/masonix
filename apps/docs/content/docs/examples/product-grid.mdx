---
title: Product Grid
description: Ecommerce-style cards with automatic responsive columns.
---

import { ProductGridDemo } from '@/components/docs/docs-live-examples';
import { DocsSourceCode } from '@/components/docs/docs-source-code';

Product grids usually need reliable card width more than shortest-column
balancing. Use `Masonry` with `columnWidth` and `maxColumns`.

<ProductGridDemo />

## Copy-paste example

<DocsSourceCode
  file="components/docs/examples/docs-example-product-grid.tsx"
  snippet="product-grid"
/>

## When to use

Use this for catalogs, collections, marketplace cards, and browse pages where
all items can render up front.

## Notes

- Keep card actions inside the render component.
- Use `itemClassName` for shared wrapper effects.
- Use `MasonryBalanced` only if product card heights vary enough to look uneven.
